# Información

Esta carpeta contiene la base de datos en formato JSON
la cual es creada a partir de la FileSystem de Node.

## Helpers 📙

En el directorio **helpers** el fichero *GuardarArchivo.js* nos ayuda a procesar el archivo pero 
es extensible a una persistencia en otra base datos (MySQL, SQlite, MongoDb, PostgreSQL)